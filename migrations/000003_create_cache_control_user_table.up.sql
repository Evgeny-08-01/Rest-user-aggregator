-- ============================================================
-- ФАЙЛ: migrations/000003_create_cache_control_user_table.up.sql
-- НАЗНАЧЕНИЕ: создание таблицы для хранения версий кеша пользователей
-- ============================================================
-- Зачем:
--   - Хранит версию кеша для каждого пользователя.
--   - При изменении подписок версия увеличивается на 1.
--   - Ключ кеша в Redis включает номер версии.
--   - Это обеспечивает мгновенную инвалидацию кеша.
--
-- Структура:
--   - user_id: UUID пользователя (PRIMARY KEY)
--   - version: INT (текущая версия, начиная с 1)
--
-- Индексы:
--   - PRIMARY KEY (user_id) — автоматически создаёт индекс
-- ============================================================

CREATE TABLE IF NOT EXISTS cache_control_user (
    user_id UUID PRIMARY KEY,
    version INT NOT NULL DEFAULT 1
);

COMMENT ON COLUMN cache_control_user.user_id IS 'ID пользователя (UUID)';
COMMENT ON COLUMN cache_control_user.version IS 'Текущая версия кеша (инкрементится при каждом изменении подписок пользователя)';